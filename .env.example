# Copy to .env (gitignored) and fill in. Loaded into process.env by the
# NodeCG extension at startup. Never commit the real file.

# start.gg Personal Access Token (https://start.gg/admin/profile/developer)
STARTGG_TOKEN=

# GeoGuessr _ncfa cookie for the presenter's spectator connection
GEOGUESSR_NCFA=
